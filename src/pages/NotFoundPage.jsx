import NotFound from '../components/NotFound/NotFound'

/**
 * NotFoundPage — renders when no route matches (the * catch-all in App.jsx).
 */
function NotFoundPage() {
  return <NotFound />
}

export default NotFoundPage
